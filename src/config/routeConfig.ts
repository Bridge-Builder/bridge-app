export enum ROUTE {
  /**
   * 메인 인덱스 페이지 라우트 주소
   */
  INDEX = '/',
  /**
   * 로그인 페이지 라우트 주소
   */
  LOGIN = '/login',
  /**
   * 회원가입 페이지 라우트 주소
   */
  LOGIN_JOIN = '/login/join',
  /**
   * 클래스 오프라인 강좌 리스트 라우트 주소
   */
  CLASS_OFFLINE = '/class/offline',
  /**
   * 클래스 온라인 강좌 리스트 라우트 주소
   */
  CLASS_ONLINE = '/class/online',
  /**
   * 강좌 등록페이지 라우트 주소
   */
  REGISTRATION = '/registration',
  /**
   * 오프라인 강좌 등록 페이지 라우트 주소
   */
  REGI_OFFLINE = '/registration/offline',
  /**
   * 온라인 강좌 등록 페이지 라우트 주소
   */
  REGI_ONLINE = '/registration/online'
}
